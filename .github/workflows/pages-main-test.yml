name: Main Pages deployment unavailable from Level 3 test branch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  locked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Public deployment is locked to refs/heads/main."
