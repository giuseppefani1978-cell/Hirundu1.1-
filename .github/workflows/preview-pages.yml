name: Legacy preview Pages disabled on 4-6-8 test branch

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  locked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "No Pages preview can deploy from this branch."
