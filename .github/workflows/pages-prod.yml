name: Legacy Pages deployment disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  locked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Public deployment is locked to the main branch workflow."
