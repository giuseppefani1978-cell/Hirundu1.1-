name: V9.2 GameFlow CI

on:
  push:
    branches: [ "ux/v9.2-gameflow" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci || npm install
      - name: Test
        run: npm test
      - name: Build
        run: npm run build
